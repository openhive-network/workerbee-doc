# WorkerBee documentation server

This repo contains the documentation server for WorkerBee. WorkerBee is an API library for Hive-based applications.

## Steps to build and run the server

```bash
pnpm install

pnpm start
```
